
注解支持多个id，如@Click(value={1,2})

TabBar 上设置
        lin:tab_text_color="#eee"
        lin:tab_text_activate_color="#00f"
    不生效


    UpdateService 下载 不采用 DownloadManager



1、 ResourceView 支持 binding


2、ptr 加 styleable

3、ptr 加纯翠网那样 转圈的头

4、ReView 用 extends ResView ,用 PtrScrollView 实现

public class ReView extends PtrScrollView {
    public ReView(Context context) {
        super(context);
        this.init();
    }

    public ReView(Context context, AttributeSet attrs) {
        super(context, attrs);
        this.init();
    }

    public ReView(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
        this.init();
    }

    private LinearLayout layout;
    private void init(){
        this.setMode(Mode.DISABLE);
        layout = new LinearLayout(this.getContext());
        layout.setOrientation(LinearLayout.VERTICAL);

        ScrollView.LayoutParams lp = new ScrollView.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT,
                ViewGroup.LayoutParams.MATCH_PARENT);

        layout.setLayoutParams(lp);

        super.addView(layout,-1,lp);
    }

    @Override
    public void addView(View child, int index, ViewGroup.LayoutParams params) {
        layout.addView(child, index, params);
    }
}