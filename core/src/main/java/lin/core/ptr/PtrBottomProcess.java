package lin.core.ptr;

import lin.core.ptr.indicator.ContentIndicator;

public class PtrBottomProcess extends PtrProcess {
    public PtrBottomProcess(PtrView view, ContentIndicator mIndicator, OnListener onListener) {
        super(view, mIndicator, onListener);
    }
}
